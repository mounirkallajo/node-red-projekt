# Mobile WebView MQTT-over-WebSocket listener.
# Keep the existing TCP MQTT listener on 1883 unchanged for Node-RED,
# OwnTracks, and the native Android service.

listener 9001 127.0.0.1
protocol websockets

